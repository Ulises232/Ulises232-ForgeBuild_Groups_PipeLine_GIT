# Variables de ejemplo para configurar ForgeBuild.
# Copia este archivo a `.env` y ajusta los valores según tu entorno.

# Backend de historial: `sqlite` (predeterminado) o `sqlserver`.
# FORGEBUILD_BRANCH_HISTORY_BACKEND=sqlserver

# Cadena de conexión para SQL Server 2019 usando python-tds.
# Ejemplo: mssql://usuario:password@servidor:1433/forgebuild
# FORGEBUILD_BRANCH_HISTORY_URL=

# Tamaño opcional del pool de conexiones cuando se usa SQL Server.
# FORGEBUILD_BRANCH_HISTORY_POOL_SIZE=8
